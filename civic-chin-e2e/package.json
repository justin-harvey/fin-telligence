{
  "name": "civic-chin-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright end-to-end harness for the CivicChain test environment. Authored to run where the site is reachable (your laptop or an allow-listed environment).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "codegen": "playwright codegen $BASE_URL",
    "auth": "playwright test auth.setup.ts",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "dotenv": "^16.4.5"
  }
}
