# This repository holds the prototype's *built output*, not its source, so
# there is nothing here for Netlify to build. The previous config declared
#   command = "npm run build"  /  publish = "dist"
# against a tree with no package.json, no source and no dist — it could not
# have deployed. Publish the committed build directly instead.
[build]
  publish = "prototype"
